# on:
#  push:
#    branches: [main]

#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#    steps:
      # - uses: actions/checkout@v4
      #- uses: actions/setup-node@v4
      #  with:
      #    node-version: 20

      # - run: npm ci
      # - run: npx quartz build

      # - run: docker exec quartz sh -c "cd /usr/src/app/quartz && git pull && npx quartz build --directory /vault --output /usr/share/nginx/html"
      # that altered build command is the one found in the build.sh script in /scripts in the container.
